// @perama: This utility provides access to the configuration for client-side components

const config = {
    downloadPort: 3023, // Make sure this matches the value in config.js
  };
  
  export default config;